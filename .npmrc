# Security: never write caret/tilde ranges. Every dependency is pinned to an
# exact version so the lockfile and manifest cannot drift, and so the API
# surface verified in docs/pi-api-notes.md stays the one we actually install.
save-exact=true
